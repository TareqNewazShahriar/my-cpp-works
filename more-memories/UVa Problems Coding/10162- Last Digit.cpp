#include<stdio.h>
#define SIZE 1000000
/****************************Global Variables***************************/
int N, k=0;
char digit[SIZE];
/*************************User Defined Functions************************/
int last_digit(int n)
	{
	if(k<)
	}
/*****************************main Function*****************************/
void main()
	{
	int i, last;
	scanf("%d",&N);
	while(N)
		{
		last=0;
		for(i=1;i<=N;i++)
			{
			if(i>k)
				last_digit(i);
			else last+=digit[i];
			}

		scanf("%d",&N);
		}
	}